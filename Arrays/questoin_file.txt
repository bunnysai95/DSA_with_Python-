Valid Palindrome: Check if a string reads the same forward and backward.
Two Sum II (Sorted Array): Find two numbers that add up to a target (since it's sorted, you move pointers from both ends).
Reverse String: Swap characters at the start and end until they meet in the middle.
Move Zeroes: Push all 0s to the end of an array while maintaining the order of other elements.
Remove Duplicates from Sorted Array: Modify the array in-place to remove repeats.
Squares of a Sorted Array: Given a sorted list of negative and positive numbers, return a sorted list of their squares.
Container With Most Water: Find two lines that together with the x-axis forms a container that holds the most water.
3Sum: Find all unique triplets in an array that give the sum of zero.
Remove Nth Node From End of List: A classic linked list problem using a "fast" and "slow" pointer.
Is Subsequence: Check if one string is a subsequence of another.